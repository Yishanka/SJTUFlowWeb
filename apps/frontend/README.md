# SJTUFlow Frontend

This directory is reserved for the local browser UI.

The current backend already exposes the local API under `/api/*`. The frontend
implementation should mount an actual app here later, likely with Vite + React
or another lightweight SPA stack.

Expected first screens:

- first-run configuration form
- briefing dashboard
- chat/task workspace
- transcript library
- skills library
