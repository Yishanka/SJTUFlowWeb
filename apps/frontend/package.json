{
  "name": "@sjtuflow/frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 -d .",
    "build": "mkdir -p dist && cp index.html app.js styles.css dist/ && rm -rf dist/assets && cp -R assets dist/assets"
  }
}
